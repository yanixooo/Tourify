# ===========================================
# Tourify Environment Variables Template
# ===========================================
# Copy this file to config.env and fill in your values
# NEVER commit config.env to version control!

# App Configuration
NODE_ENV=development
PORT=3000

# MongoDB Database
# Replace <PASSWORD> with your actual MongoDB password
DATABASE=mongodb+srv://username:<PASSWORD>@cluster.mongodb.net/tourify?retryWrites=true
DATABASE_PASSWORD=your_mongodb_password

# JWT Authentication
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-ultra-secure-and-very-long-secret-key-minimum-32-characters
JWT_EXPIRES_IN=90d
JWT_COOKIE_EXPIRES_IN=90

# Email Configuration
# For development, use Mailtrap (https://mailtrap.io)
# For production, use SendGrid, Mailgun, or your SMTP provider
EMAIL_HOST=sandbox.smtp.mailtrap.io
EMAIL_PORT=587
EMAIL_USERNAME=your_mailtrap_username
EMAIL_PASSWORD=your_mailtrap_password
EMAIL_FROM=noreply@tourify.com

# Stripe Payment Configuration
# Get your keys from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Optional: For webhook testing locally, use Stripe CLI
# stripe listen --forward-to localhost:3000/api/v1/bookings/webhook
